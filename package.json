{
  "name": "crm",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "docker compose up --build",
    "dev:down": "docker compose down",
    "dev:down:volumes": "docker compose down -v"
  }
}
